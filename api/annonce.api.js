

const fetchAnnonce = async (method, url, body = null, token) => {
const host = process.env.COMMAND_API_IP || 'http://localhost';
const port = process.env.COMMAND_API_PORT || 3000;
    
}

module.exports = fetchAnnonce;
