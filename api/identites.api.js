

const fetchIdentite = async (method, url, body = null) => {
const host = process.env.VENDEURS_API_IP || 'http://localhost';
const port = process.env.VENDEURS_API_PORT || 3001;
    
}

module.exports = fetchIdentite;
